/*
package com.example.weatherapp.repository
import com.example.weatherapp.api.WeatherService
import com.example.weatherapp.model.WeatherResponse

class WeatherRepository(private val apiService: WeatherService) {

    suspend fun fetchWeather(latitude: Double, longitude: Double): WeatherResponse {
        return apiService.getWeatherByLocation(latitude, longitude)
    }

    suspend fun searchWeatherByCity(city: String): WeatherResponse {
        return apiService.getWeatherByCity(city)
    }
}
*/
